# Allow all search engines to crawl all pages
User-agent: *
Allow: /

# Crawl-delay (optional, helps prevent overloading the server)
Crawl-delay: 1

# Dynamic sitemap (auto-updates with new portfolios)
Sitemap: http://localhost:3001/sitemap.xml
